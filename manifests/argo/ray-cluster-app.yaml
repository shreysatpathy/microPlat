apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ray-sample-cluster
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  destination:
    namespace: ml-dev
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: https://github.com/your-org/your-repo.git  # update to actual repo
    targetRevision: HEAD
    path: charts/ray-cluster
  syncPolicy:
    automated: {}
